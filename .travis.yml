language: node_js
sudo: false
node_js:
  - "6"
install:
  - npm install
script:
  - npm run build
deploy:
  provider: pages
  local_dir: build
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: master